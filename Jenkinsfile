pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Build komutları
            }
        }
        stage('Test') {
            steps {
                echo 'Testing the project...'
                // Test komutları
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
                // Deploy komutları
            }
        }
    }
}
